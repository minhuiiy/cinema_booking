PORT=3000

# SMTP configuration for sending QR via email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=minhhuy5929@gmail.com
SMTP_PASS=Ttmh@080204010880
SMTP_SECURE=false

# Admin API key to protect /admin/set-admin endpoint
ADMIN_API_KEY=replace-with-a-strong-secret

# Firebase Admin credentials
# Point to your service account JSON file path for local development
# Example (Windows): C:\\path\\to\\firebase-service-account.json
# Example (macOS/Linux): /path/to/firebase-service-account.json
GOOGLE_APPLICATION_CREDENTIALS=